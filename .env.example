PORT=4000
JWT_SECRET=una_clave_fuerte_aqui
JWT_EXPIRES_IN=1d

# Puedes definir DB_URL directamente o definir las partes por separado
# Opción A: URL completa (ejemplo)
# DB_URL=mysql://cotizador:TuPasswordFuerte@localhost:3306/cotizador

# Opción B (recomendada): definir las partes y generar DB_URL con el script
DB_USER=cotizador
DB_PASS=TuPasswordFuerte
DB_HOST=localhost
DB_PORT=3306
DB_NAME=cotizador

# DB_URL will be generated if you run ./scripts/build-db-url.sh
DB_URL=

SMTP_HOST=smtp.ejemplo.com
SMTP_PORT=587
SMTP_USER=usuario
SMTP_PASS=contrasena
