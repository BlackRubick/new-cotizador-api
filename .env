# Copia este archivo a .env y edítalo con tus credenciales reales
PORT=4000
NODE_ENV=production
JWT_SECRET=una_clave_fuerte_aqui
JWT_REFRESH_SECRET=otra_clave_fuerte_diferente_aqui
JWT_EXPIRES_IN=1d
FRONTEND_URL=http://3.137.142.144
# Puedes definir la URL completa o las partes por separado.
# Prisma necesita DB_URL cuando corre. Opciones:
# 1) Edita DB_URL abajo con la URL completa (mysql://USER:PASSWORD@HOST:PORT/DATABASE)
# 2) O define las partes DB_USER/DB_PASS/DB_HOST/DB_PORT/DB_NAME y ejecuta `./scripts/build-db-url.sh` para generar DB_URL en este archivo.
# Formato de ejemplo:
# DB_URL=mysql://cotizador:TuPasswordFuerte@localhost:3306/cotizador

# --- Opciones por partes (recomendada para desarrollo)
DB_USER=cotizador2
DB_PASS=cesar
DB_HOST=localhost
DB_PORT=3306
DB_NAME=cotizador2

# DB_URL will be generated if you run ./scripts/build-db-url.sh
DB_URL=mysql://cotizador2:cesar@localhost:3306/cotizador2
SMTP_HOST=smtp.ejemplo.com
SMTP_PORT=587
SMTP_USER=usuario
SMTP_PASS=contrasena
